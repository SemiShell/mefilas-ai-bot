version: '3.8'

services:
  mefilas-bot:
    # 'build: .' 表示使用当前目录下的 Dockerfile 来构建镜像
    build: .
    # 给容器起个名字，方便管理
    container_name: mefilas-bot
    # 设置重启策略为：除非手动停止，否则总是重启
    restart: unless-stopped
    # 从 .env 文件中加载环境变量
    env_file:
      - .env
